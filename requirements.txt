# ── Core ML ───────────────────────────────────────────────────────────────────
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.37.0
accelerate>=0.26.0
optimum>=1.16.0
bitsandbytes>=0.41.0

# ── ASR ───────────────────────────────────────────────────────────────────────
faster-whisper>=1.0.0

# ── MT ────────────────────────────────────────────────────────────────────────
sentencepiece>=0.1.99
sacremoses>=0.1.1

# ── TTS ───────────────────────────────────────────────────────────────────────
# "TTS" (original Coqui) is unmaintained and has no wheels for Python >=3.12.
# "coqui-tts" is the maintained fork (idiap) — same `import TTS` API, drop-in.
coqui-tts>=0.24.0
edge-tts>=6.1.0

# ── Audio ─────────────────────────────────────────────────────────────────────
librosa>=0.10.0
soundfile>=0.12.1
audioread>=3.0.0
pydub>=0.25.1

# ── Evaluation ────────────────────────────────────────────────────────────────
jiwer>=3.0.0
sacrebleu>=2.3.1

# ── Data ─────────────────────────────────────────────────────────────────────
datasets>=2.16.0

# ── Utils ─────────────────────────────────────────────────────────────────────
pyyaml>=6.0.1
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.66.0
loguru>=0.7.0
